# Build artifacts
dist/
dist-generator/
dist.zip
# Generated by `make sbom` / the drift gate; the release publishes them as
# assets rather than committing them. Anchored, so a fixture with one of these
# names under samples/ is still tracked.
/dist.zip.cdx.json
/dist.zip.cdx.html
/drift-check.cdx.json
/drift-check.html
.vite/

# Dependencies
node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Test
coverage/
screenshots/*.pdf

# AI tools
_bmad-output/
_bmad/
openspec/
.agent/
.claude/
.codex/
